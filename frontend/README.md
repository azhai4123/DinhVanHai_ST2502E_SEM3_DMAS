# BattleGame Frontend

This is a minimal React app that calls the Azure Functions API `getassetsbyplayer` and shows the report.

Run locally:

```bash
cd frontend
npm install
npm start
```

Set API base URL with `REACT_APP_API_URL` environment variable if needed.
